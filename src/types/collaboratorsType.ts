import { ReactNode } from 'react';

export type CollaboratorsType = {
  length: ReactNode;
  id: number;
  name: string;
  neighborhood: string;
  city: string;
  street: string;
  number: string;
  phone: string;
  arrivalRouteId: number;
  outboundRouteId: number;
  company: string;
  department: string;
  position: string;
  createdAt: string;
  updatedAt: string;
};
